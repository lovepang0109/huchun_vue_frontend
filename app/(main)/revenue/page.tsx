import Revenue from "./Revenue"; // Use PascalCase for the component name
import "@/public/css/base.style.css";

export default async function ContentRoute() {
  return <Revenue />; // Use camelCase when using the component in JSX
}
