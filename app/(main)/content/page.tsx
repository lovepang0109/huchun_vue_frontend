import ContentLibrary from "./contentLibrary"; // Use PascalCase for the component name
import "@/public/css/base.style.css";

export default async function ContentRoute() {
  return <ContentLibrary />; // Use camelCase when using the component in JSX
}
