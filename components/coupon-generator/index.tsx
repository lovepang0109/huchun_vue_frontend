import { Modal, ModalBody } from "react-bootstrap";
interface props {
  show: boolean;
  onClose: any;
}
const CouponGenerator = ({ item, readonly }: any) => {
  return <>CouponGenerator</>;
};

export default CouponGenerator;
